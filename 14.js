var longestCommonPrefix = function(strs) {
    if(strs.length < 1){
        return "";
    }
    if(strs.length === 1){
        return strs[0];
    }

    let first = strs[0]
    for(var i = first[0])
    
};